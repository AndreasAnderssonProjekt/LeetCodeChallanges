def lengthOfLastWord(self, s):
        
        start = len(s) - 1
        
        while s[start] == " " and start > 0:
            start -= 1
            
        
        end = start
        
        while s[end] != " " and end >= 0:
            end -= 1

        return start - end
